{
  "private": true,
  "name": "devmark",
  "license": "MIT",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "yarn turbo run dev",
    "bootstrap": "rimraf node_modules && yarn"
  },
  "devDependencies": {
    "concurrently": "^7.3.0",
    "rimraf": "^3.0.2"
  },
  "dependencies": {
    "@nestjs/cli": "^9.0.0",
    "turbo": "^1.0.6"
  }
}
